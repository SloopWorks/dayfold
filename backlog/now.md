# Backlog — Now

## ⚠ Time-sensitive (hard dates — keep pinned at top)

*(empty)*

Stage: **template — awaiting bootstrap.** The live work queue is
`planning/workstreams.md` once bootstrapped; this file holds operator-facing
immediates.

## Operator actions pending

- [ ] Run bootstrap: open a session here and say
  "Bootstrap this project: \<idea\>" (agent follows `BOOTSTRAP.md`).

## Loop state

- Not armed. After bootstrap: "run a loop iteration" (one-shot) or
  "run a loop iteration every 1hour" (session cron; re-arm after restarts,
  or use a cloud schedule for durability).

## Done this period

*(empty)*
