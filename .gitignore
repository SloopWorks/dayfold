# node / TS
node_modules/
dist/
*.tsbuildinfo
.vercel/

# kotlin / gradle
.gradle/
.kotlin/
build/
*.iml
local.properties

# env / secrets (NEVER commit)
.env
.env.*
!.env.example

# os / editor
.DS_Store
*.log
.idea/


# Firebase client config — injected per-env, not committed (public repo). See specs/auth-s2-google-signin-design.md
apps/androidApp/google-services.json
**/GoogleService-Info.plist

# rk snapshot batch/dashboard output (regenerate: agent-dev-loop.md "Golden dashboard")
apps/client/.rk-snapshots/
