# Decisions Index

Index of decision records. The ADRs themselves live in `adr/`. Once
accepted, an ADR is immutable — supersede it with a new ADR rather than
editing it.

| ADR | Title | Status |
|-----|-------|--------|
| 0000 | ADR Template | Template |
| 0001 | Repo Markdown as Source of Truth; Memory Systems as Working Memory | Accepted 2026-06-18 |
| 0002 | Execution & Tracking Model — Phase-Gated, Agent-Operated, Operator-Escalated | Accepted 2026-06-18 |
| 0003 | Planning-Loop Operating Model — Autonomous Waterfall Deepening with Confidence-Gated Decisions | Accepted 2026-06-18 |
| 0004 | Product Framing — Calm Family Briefing Surface, Content-API-Fed, Adults-Only MVP | Accepted 2026-06-18 (adults-only clause: supersession proposed in 0005) |
| 0005 | Minor Accounts (14+) Permitted at MVP, No Email Integration for Minor Profiles | Proposed 2026-06-18 (operator-gated; pending counsel) |
| 0006 | Event Hubs — Co-Equal Curated-Dossier Surface, App-Owned, Push-Curated | Accepted 2026-06-18 |
| 0007 | Prototype Scope — Operator-Driven Dumb Renderer | Accepted 2026-06-18 |
| 0008 | Design-First — Hi-Fi UI/UX Mockups Precede Deep Planning and Build | Accepted 2026-06-18 |
| 0009 | Design System — Material 3 Expressive, Adaptive, Compose | Accepted 2026-06-18 |
| 0010 | Auth & Family-Tenancy Architecture (Firebase Auth, M:N membership, RFC 8628 device grant) | **Superseded by 0011** (2026-06-18) |
| 0011 | Auth & Family-Tenancy Architecture (Hardened) | Accepted 2026-06-18 (supersedes 0010; post-5-agent-review) |
| 0012 | Agent-Operated Build & Deploy — Autonomy Boundaries | Accepted 2026-06-18 (operator-set: full prod autonomy + safety rails, budget cap, browser-after-login) |
| 0013 | Client Architecture — KMP/CMP Shared UI + redux-kotlin 1.0.0-alpha1 | Accepted 2026-06-18. **Build pin → `1.0.0-alpha01`** (INB-11 superseded 2026-06-19: operator owns/maintains reduxkotlin, so alpha-churn risk is moot; directive: latest APIs + `f(store.state)→UI`). All modules on Kotlin 2.3.20; `store.selectorState{}` (f(store.state)→UI) + devtools wired + verified on Pixel/emulator. |
| 0014 | Private On-Device Trigger Engine (geo/time/activity) | Accepted 2026-06-18 (triggers=metadata; device matches locally; live position never leaves) |
| 0015 | End-to-End Encryption | **Proposed — scoped to M1** (INB-10: M0 = PLAINTEXT; live E2EE is an M1 option gated by ADR 0017) |
| 0016 | Two-Way Interactive Pull-Loop (reserved, bounded-now) | **Proposed** 2026-06-18 (additive; reasoning stays in the key-holding loop; reserved actions[]/intents) |
| 0017 | E2E Key-Authenticity + Deploy Trust-Root Boundary | **Proposed** 2026-06-18 (M1 security gate; from the 6-agent review — fake-key MITM + trust-root concentration) |
| 0018 | API Host — TypeScript on Vercel | Accepted 2026-06-18 (INB-9; CLI/client stay Kotlin; types codegen from schema; Postgres via pooler) |
| 0019 | Client Observability & Tooling (devtools/snapshots/CLI) | **Accepted** 2026-06-19 (operator-directed) — redux-kotlin-devtools enhancer + in-app drawer LIVE (verified on-device) + Compose snapshots; golden-diff/remote/CLI remaining |
| 0020 | Offline-First Client Data + Freshness (DB-as-source-of-truth) | **Proposed** 2026-06-19 — network→DB(SQLDelight)→store→UI unidirectional; instant offline cold-start; foreground poll ~30–60s (push later); background WorkManager/BGTask. Build slice pending (shipped client is in-memory) |
| 0021 | Auth Pulled Into Active Build — S1–S6 Decomposition + Dev-Token Posture | **Accepted** 2026-06-19 (extends 0011; re-confirms its sequencing trigger). Build order S1→S3→S2→S4→(S5,S6); non-breaking household-token coexistence (cutover at S3); dev-token local-only. S1 build in progress. |
| 0022 | Typed Content Library, Detail View & the Fold Gesture | **Accepted** 2026-06-19 (operator; INB-15/16/17/18) — 6 typed content types (one renderer → Now/Hub/Detail), container-transform fold gesture, first-class provenance+privacy, **Dayfold** M3 theme. **D2 = extend `briefing_cards` in place** (unify→M1); phone mockups signed off (ADR 0008 cleared; adaptive queued); M0 ships all 6 types. Fills the gap the designs mis-cite as "ADR 0015". Epic: `planning/content-detail-epic.md` |
| 0023 | S2 Firebase Identity — Google + Apple, Phone-OTP deferred | **Accepted** 2026-06-20 (operator-directed) — extends 0011/0021, narrows the S2 provider set to **Google + Apple** (free tier, no Blaze). **Phone-OTP deferred** → removes the SMS spend ceiling + the SMS-fraud/SIM-swap surface; clears the S2 vendor/cost gate. ADR 0011 architecture intact (backend-minted tokens, app-driven link, per-device revoke, Apple `revokeToken`). S5/S6 build renders Google+Apple only (phone/OTP screens stay designed-not-built). |
| 0024 | Settings Privacy Posture — Tiering Boundary, Never-Syncs Rule, Telemetry Consent & E2EE Line | **Proposed** 2026-06-21 (operator-gated; guardrail-class #3/#4). Privacy posture of the settings surface only (storage shape = HIGH-confidence in `specs/account-and-settings-design.md` §6, not ADR). Never-syncs list (location-permission state, theme, a11y, devtools); telemetry emission gated on a device-local OFF default; E2EE line (only named places encrypted; rest plaintext). Composes with 0014/0015/0019/0020. Spec: `specs/account-and-settings-design.md` |
| 0025 | Auth Abuse-Control — Rate Limits & Lockout Constants | **Accepted** 2026-06-21 (operator-directed) — records the S3/S4 anti-abuse constants as a single source of truth: device authorize 10/600s·IP, token poll 600/600s·IP, owner-mint 20/600s, invite caps ≤10 active/≤20 pending, and the **invite-redeem lockout = 5 failed / 15 min → 15-min lock** (the `invitelocked` screen's cooldown). Composes with 0011/0021. |
| 0026 | Package & App-ID Naming — `com.sloopworks.*` Umbrella | **Accepted** 2026-06-21, revised post-4-agent review (operator-gated; branding/vendor convention) — `com.familyai.*` → store id **`com.sloopworks.dayfold`**; namespaces `…dayfold.android` (app) / `…dayfold.client` / `…dayfold.cli` / `…dayfold.schema`. Full sweep incl. env (`DAYFOLD_API`), npm (`@sloopworks/*`), CLI binary `dayfold`, client-id `dayfold-cli`, runtime paths (`~/.dayfold`, re-login no-migrate), bundled `api/index.js` redeploy, CI guard. **No Firebase/OAuth/iOS/web exist yet** → review corrected the "OAuth re-register" cost to a *future* trigger. **Timing gate: after `auth-account-delete-soft`, STRICTLY before S2 identity provisioning**; carry `cl-integrate` worktree + `auth-signout-account` across via merge. |
| 0027 | S2 Firebase Verify — Direct JWKS, Emulator-in-CI, Android-First | **Proposed** 2026-06-22 (operator-directed; refines 0011, extends 0023). Verify Firebase ID token by **direct JWKS** (`jose`, no Admin SDK / no service-account secret); identity binds to underlying provider uid (`firebase.identities[provider][0]`, not Firebase uid). New always-on `POST /auth/firebase` mints OUR tokens; `dev-token` kept as test seam. **Test topology:** hermetic signed-RS256 units + **Firebase Auth Emulator in CI** (emulator tokens unsigned → env-gated emulator-decode mode, refused in prod/preview); preview = manual QA. **Targets: Android only** (desktop/iOS keep dev-token). Backend slice built + 12 tests green. |
| 0029 | CLI / Token Resource-Scoped Grants — Per-Hub Read/Write Scoping | **Accepted** 2026-06-23 (operator-directed + accepted in the S6-D auth review; per-hub/resource scoping). Extends 0011/0021, composes 0022/0025. `credential_grants` table + global/resource-qualified scope strings resolved per request (never in the token); central `requireScope` gate (fixes read-enforcement gap); approve-screen per-hub read/write picker (default all-content read+write). **Rides with the content-API + CLI-verbs slice** (needs the hub resource model); S6-D ships interim global `content:read+write`. Review: `docs/superpowers/specs/2026-06-23-auth-s6d-device-approval-design.md` |
| 0030 | Per-Member Hub & Card Visibility — Restricted Resources at MVP | **Accepted** 2026-06-23 (operator-directed; schema/scope review; both adversarial rounds applied; INB-21 ratified owner-NOT-auto-permitted [A] + author-trusted card posture [A]) — resolves review-gap G3. Hubs get `visibility` (`family`\|`restricted`) + `created_by` + a hubs-only `resource_visibility` allow-list; cards carry a flat author-stamped `audience[]` (round-2: no inheritance/materialization/fan-out). Read-path filter, omit-don't-403; load-bearing `resource_visibility`→`hubs.updated_at` touch-trigger + client cache-wipe on tenancy 401/404 (round-1 P0 fixes). Extends 0011/0006, composes 0029/0022; shares ADR 0029's `hub:<id>` resource model (visibility=human-read, scope=credential-write). **Open → INB-21:** owner-visibility default (owner NOT auto-permitted, proposed) + ratify the author-trusted (not server-enforced) card-vs-hub posture `[pending-ratify]`. Spec: `specs/domain-model/scope-and-access-model.md`; boundary: `specs/mvp-feature-boundary.md` |
| 0028 | Geocoding Strategy & Location-Privacy Tiers | **Accepted** 2026-06-22 (operator-directed) — locks the principle: **geocoding never server-side; server stays zero-knowledge** (E2EE-forward; `places.lat/lng` is ciphertext per 0015). Coordinate from an authoritative source, **never the LLM** (geofence-unfit). M0 = author-side **T1 author-supplied / T2 CLI offline** only. **Deferred + revisited once CLI encoding (0015 encrypt-before-upload) lands + post-MVP:** T3 on-phone GPS-capture · T4 BYO geocoder · T5 first-party opt-in (last resort, never default) · third-party provider choice (**OpenCage/Geoapify, never Google**). `place_ref`/`places` geocode-once (iOS 20-region cap). Claim wording `[pending-counsel]`. Refines 0014, composes 0015/0024. Spike: `docs/superpowers/specs/2026-06-21-cl-9-map-render-spike.md` |
| 0031 | CLI Distribution — First-Party Homebrew Tap (Zero-Config JVM) | **Accepted** 2026-06-25 (operator-directed "approve 0031"; pipeline implemented — `release-cli.yml` + formula + runbook; operator setup gates open — distribution channel + release automation + a public-distribution/license implication). One-line `brew install sloopworks/tap/dayfold`, zero user config: **Option A** = the `application` dist + formula `depends_on "openjdk"` + an env-script pinning `JAVA_HOME` (brew installs Java; no `JAVA_HOME`/PATH edits). Tag `cli-v<semver>` → CI builds `distTar -PcliVersion`, publishes a GitHub Release, and **auto-bumps the tap formula**. Avoids the `rk` `jpackage` `.app` symlink trap (ships a real `bin/dayfold`, landed #76); rejects `.app`/`.pkg` + notarization. jlink (self-contained) recorded as the upgrade path; native rewrite deferred. **Operator gates:** license/public-distribution decision (→ ADR 0032), create the tap repo, `HOMEBREW_TAP_TOKEN` secret, accept. Composes 0018/0026/0012. Spike: `research/2026-06-25-spike-cli-homebrew-distribution.md` |
| 0032 | Licensing & Open-Source Posture — Apache Client/CLI + AGPL Server, Hosted-SaaS Monetization | **Proposed** 2026-06-25 (agent-drafted from the licensing research; **operator-gated + `[pending-counsel]`**). Closes ADR 0031's license gate + `OQ-license`. **Open-source for showcase/trust = GO** (near-zero opportunity cost — revenue thin regardless + self-host doesn't overlap the convenience ICP; no embedded secrets, security is server-enforced not obscurity). **Per-component:** Apache-2.0 (client/CLI/schema) + **AGPL-3.0-or-later** (server — optics/insurance not moat; AGPL>BSL since no hyperscaler forks a family app + AGPL stays OSI-approved) + **G1 "brains" closed by not publishing** (separate private repo). **Monetize via hosted SaaS only**; open-core/sponsors thin secondary; reject services. **DCO** contributions (CLA only if dual-license intended — decide first). **BLOCKING pre-flight:** full-history secret scan + rotate + push protection + `SECURITY.md` before public. Composes 0004/0015/0031. Report: `research/2026-06-25-licensing-open-source-strategy.md` |
| 0033 | Database Migration Management — Tracked, Idempotent, Verified | **Accepted** 2026-06-26 (operator "I accept 0035, 0033"); runner implemented — `schema_migrations` table (migration `0012`) + `scripts/migrate.mjs` (`db:migrate`/`--dry-run`/`--backfill`) + pure-logic tests + drift fixtures updated; prod run (incl. one-time `--backfill`) is the deploy step under ADR 0012 rails. Was Proposed 2026-06-25 (agent-drafted from the 2026-06-25 prod auth outage; operator-gated — changes the deploy/migration process + adds a tracking table). Migrations are hand-applied (`psql -f`), non-idempotent, with **no record of what's applied** → prod's AUTH-epic (`0002`/`0003`/`0008`) was never applied, 500'ing real sign-in + device-login; the first fix hit the wrong Neon branch. **Decision: Option A — a `schema_migrations` tracking table + an in-repo `pg`-only runner (`scripts/migrate.mjs` → `npm run db:migrate`)** that applies pending files in order, one txn each, skips applied, re-run-safe (`--dry-run`); one-time backfill for existing DBs; migrations stay forward-only plain SQL (no `IF NOT EXISTS` retrofit). Rejected B (idempotent-all; keeps emergency SQL) + C (manual #93 gate). Composes #91 (drift-guard) + #93 (`schema-drift.mjs`) + ADR 0012 (agent runs `db:migrate` on prod under the rails). |
| 0034 | Mobile Release Pipeline — Alpha/Beta/Prod to Google Play | **Accepted** 2026-06-26 (operator "inb 23 approved"; agent-drafted from a 3-agent CI/CD best-practices review; **operator-gated** — platform/vendor + external app publishing + spend, beyond ADR 0012 rails). **G5 posture ratified** (all tracks→prod Vercel, AUTH-S3, never bake `HOUSEHOLD_SECRET`/`DEV_AUTH_SECRET`); G1–G4 remain one-time operator setup. 3-track train: **PR merge→`main` → Play `internal` (alpha)**; tag **`android-beta-v*` → `beta`**; tag **`android-v*` → `production` as a DRAFT** (operator rolls out — external-action guardrail). Implemented + locally-verified: env-driven **upload-key signing** + monotonic **versionCode=run#+1000** in `:androidApp` (backward-compatible — unsigned without secrets), **`release-android.yml`** (build with `bundleRelease`, publish with **fastlane `supply` CLI** — rejects gradle-play-publisher [AGP-9 risk] + third-party actions [repo posture]), and a PR **`assembleDebug` smoke** job in `ci.yml`. **Inert until** operator gates: G1 keystore, G2 real `google-services.json`, G3 Play account+service-account (spend; first AAB manual), G4 store listing/data-safety, G5 API-env+auth decision (all tracks→prod Vercel; never bake `HOUSEHOLD_SECRET`/`DEV_AUTH_SECRET`). Gaps: G6 promote-artifact, G7 R8, G8 iOS (needs Xcode host first), G9 first-run SDK validation. Mirrors **ADR 0031** posture; composes 0012/0026/0023/0027/0021. Runbook: `processes/mobile-release.md`. |
| 0037 | CLI Continuous "Edge" Channel + `dayfold update` | **Accepted** 2026-06-26 (operator-directed in-session: "Rolling edge on every main push" + "`dayfold update` → brew + version nudge"). **Extends ADR 0031** (does not supersede — the tag-driven stable channel is unchanged). Adds: (1) a **continuous edge channel** — every push to `main` touching `apps/cli/**` or `packages/schema/**` builds the dist and refreshes a single **`cli-edge` GitHub pre-release** with a stable asset name (`dayfold-edge.tar`); uses only `github.token` (no operator gates), PRs never publish, and `releases/latest` (stable) ignores the prerelease; (2) **`dayfold update`** — delegates to `brew upgrade dayfold` when brew-managed, else prints install/upgrade instructions, after reporting the latest stable semver vs the running version; (3) a **throttled (once/24h), TTY-only, fail-silent update nudge** after interactive `push`/`pull` (skips dev/edge builds, `CI`, `DAYFOLD_NO_UPDATE_CHECK`). Stable releases + the Homebrew tap formula remain **tag-driven (ADR 0031)**; the brew install/upgrade path stays inert until 0031's operator gates (tap repo + `HOMEBREW_TAP_TOKEN` + license) close. Composes 0031/0018/0012. Workflow: `.github/workflows/release-cli-edge.yml`; runbook: `processes/cli-release.md`. |
| 0038 | Two-Way Collaborative Content — Direct Member Mutation Primitive (interactive to-do) | **Accepted** 2026-06-29 (operator ratified the two-way bundle; INB-25 closed: accept + member scope = global `content:write`). Was **Proposed** 2026-06-27 (agent-drafted from a 4-agent brainstorm + 2 adversarial rounds; **operator-gated** — automation-autonomy boundary + customer-data write path + E2EE posture + scope). Dayfold's **first two-way data-flow** primitive: a **content-delta** (direct member mutation the server mechanically relays, never reasons over) **beside** ADR 0016 `intents`, reusing ADR 0020's reserved **outbox**. Load-bearing M0 reservation = **client-minted ULID `id` on `ChecklistPayload` items** (today id-less). Conflict resolution is **client-side per-item LWW** (`done`-triple, `(doneAt,actorId)` stamp; loop-authoritative non-`done` fields take-remote; **strict LWW, no done-wins**; HLC reserved) over a **server-opaque relay** — the only shape that survives the M1 E2EE ciphertext flip (AAD `(family_id,id,version)`) without a re-model. Transport = existing **whole-block PUT** + `If-Match`→**412** + merge-retry + **`op_id` idempotency** (M0-included/M1-required). Closes two latent defects before members can write: **visibility-on-write** (+uniform-404) and **410-on-tombstoned-block** (no `deleted_at=NULL` resurrection). Provenance = server `credential_id`→user cleartext join + client `doneBy` display. **Toggle-only** first slice; UI gated on ADR 0008 mockup. Reserves op-log/granular transport + budget/RSVP generalization. **Open → INB-25:** accept ADR + member write-scope (global `content:write` vs per-hub for a read-only member). Design + audit trail: `specs/two-way-collaborative-content-design.md`. |
| 0039 | Two-Way Mutation Engine — Typed-Op Spine, Two Channels, Lifecycle Ops | **Accepted** 2026-06-29 (operator ratified the two-way bundle; INB-26 closed: R2, W2 visible-hubs-only, W5 hide local-only-first, W3/cost via 0041/0042). Was **Proposed** 2026-06-28 (agent-drafted from a 6-specialist review panel + 1 adversarial round; **operator-gated** — automation-autonomy + customer-data write path + E2EE posture + scope). **Generalizes ADR 0038** (its first instance) into the write engine all member-write features ride: one client **outbox of idempotent typed ops** (`toggle`/`upsertBlock`/`setMedia`/`delete`/`hide`/`submitContext`) → one **`POST /mutations`** batch endpoint → **two channels** sharing the queue — **content-delta** (server relays an opaque row, never reasons) for W1/W2/W4/W5, and **intent** (server enqueues opaquely; the **key-holder** AI loop reasons) for W3 "add context". `op_id` drives a generalized **`op_log`** idempotency table. **Delete** = soft-delete + tombstone (operational) vs erasure-on-request (crypto-shred + **refcounted** blob purge); **hide** = per-member, self-scoped, orthogonal to the ADR 0030 ACL, **local-only first**, **in-ciphertext on sync** (a cleartext per-member hide table is a behavioral who-hid leak). Media stays **inside the opaque payload** (validation plaintext-M0-gated + client-side at M1 → M1 degrades to client-trust + server size/count cap). Generalizes ADR 0038's two must-fixes (**visibility-on-write** +uniform-404; **410-on-tombstone**) to **every** mutation. **Dumb-server invariant** preserved per op (cleartext columns only, never `payload_blob`). **Reserve the shape now / build a minimal slice** (toggle + delete + local-hide); defer the `/mutations` batch+DAG, W1 media, W2 authoring, W3 intents to their own slices/ADRs. Client outbox is **egress-only**; pending/failed via a `local_state` column (preserves ADR 0020 unidirectionality). Spawns sibling ADRs (authoring/`content:delete` authz; member-media/0036-Phase-2; 0016-amendment intents+constitution gate; schema-evolution ruleset; 0015/0025/0020 amendments). **Open → INB-26** (6 operator decisions). Composes 0038/0016/0020/0015/0017/0029/0030/0025/0033/0022/0008. Design: `specs/two-way-engine-and-content-management-design.md`. |
| 0040 | Freshness Spectrum & Tombstone Retention — Daily-Poll through Realtime on One Cursor | **Accepted** 2026-06-29 (operator ratified the two-way bundle; cursor/full-resync contract + tombstone floor build now, push/realtime stay deferred drop-ins). Was **Proposed** 2026-06-28 (agent-drafted from the heterogeneous-cadence review; **partly operator-gated** — cursor/watermark/full-resync contract is agent-buildable; push credentials [FCM/APNs] + any held-connection realtime vendor are operator gates). **Extends/refines ADR 0020** — promotes its R2/R3 from "polling now, push deferred" into a **client-cadence spectrum contract**. Cadence is a **stateless per-device client policy**; the keyset `(updated_at,id)`+tombstone `/sync` is the single read path, so daily / foreground-poll (~45s, M0) / background (WorkManager·BGTask) / push-woken / held-connection-realtime devices all resume from their own cursor and a family **mixes** them; each tier is a **trigger swap, not a new dataflow**. **Load-bearing rule (G1):** arbitrarily-stale clients stay correct via a **stale-cursor → full-resync directive** on `/sync` (client wipes + rebuilds — contract now) + a **fixed tombstone-retention floor** (≥60–90d; new content-tombstone GC arm on `/cron/sweep`); a **per-credential sync-watermark** GC is the reserved aggressive-purge optimization (dormant/revoked creds excluded so one dead device can't pin retention). **Push = contentless E2EE-safe *signal*** ("family X changed, sync now" — never content; the push vendor can't read ciphertext anyway), emitted **debounced per-family + jittered** (one wake per burst; no thundering herd). **Realtime ceiling:** push-seconds fits Vercel serverless; sub-second held-connection (WS/SSE) needs a **managed pub/sub vendor** (spend) — only a faster trigger, cursor sync unchanged. Two-way is cadence-agnostic for correctness (client LWW + `op_id` idempotency converge regardless of *when* stamps exchange). **M0 = poll+background + the full-resync/floor contract**; watermark-GC, push, realtime deferred drop-ins. Composes 0020/0038/0039/0015/0017/0029/0030/0024/0018/0025/0034/0023. Resolves `OQ-freshness-spectrum`. Design: `specs/two-way-engine-and-content-management-design.md` §10.5. |
| 0041 | Constitution Amendment — Bounded Member-Authored AI Commands | **Accepted** 2026-06-29 (operator explicitly accepted — HARD GUARDRAIL tier — "Accept — W3 builds (flagged)"; amended text applied to `business-constitution.md`; W3 ships EXPERIMENTAL/flagged, promotion = separate operator decision). Was **Proposed** 2026-06-28 (operator-directed: "use free form composer — essentially a remote command for the backing AI"; **operator-gated — HARD GUARDRAIL: scope + constitution change**, highest tier). **Narrows — does not delete — the constitution's "not an open-ended AI chatbot" line** so a member may submit free-form natural language as a **bounded, async, no-reply command** to the key-holding AI loop (which authors a result card later, e.g. "research dorm rooms and make a card"). Permitted only behind **five fences**: (1) **no conversation/reply ever** (output is a filed card, the load-bearing non-chatbot line); (2) **async, not real-time**; (3) **bounded to the submitter's entitlements** — acts only within the member's own visibility, **no destructive/audience-widening side-effects** (ADR 0042 enforces); (4) **key-holder reasoning only** (never hosted server-side LLM — ADR 0015/0016 §3); (5) **server still renders, never reasons**. Ships **EXPERIMENTAL / flagged / testable, not committed** (operator flagged a possible overstep, product direction TBD; promotion to shipped is a separate decision). Fires ADR 0016 §4's gate; pairs with **ADR 0042** (technical activation). Includes proposed replacement text for `business-constitution.md` (applied on acceptance — not edited until then). A *conversational* surface (replies/threads/real-time) stays out of scope, needs its own superseding ADR. Composes 0016/0039/0015/0017. |
| 0042 | Activate the Intents Channel for W3 — Bounded Member Commands to the Key-Holder Loop | **Accepted** 2026-06-29 (0041 accepted → blocker cleared; cost constants accepted; W3 builds EXPERIMENTAL/flagged, surface gated on Add-Context mockup). Was **Proposed** 2026-06-28 (operator-directed: W3 = free-form composer, bounded-to-submitter, key-holder-only; **operator-gated** — automation-autonomy + customer-data write path + E2EE posture + spend). **Fires ADR 0016's revisit trigger** + **builds within ADR 0041** (blocked on 0041 acceptance — if rejected, W3 stays structured-bounded). Concretizes ADR 0039's **Channel B**: the `intents` table + endpoints (member `submitContext` via `/mutations`; loop pulls `FOR UPDATE SKIP LOCKED` via `intents:resolve`, authors a result card, stamps `source_context_id`; folds into `/sync`). Safety core = **bounded-to-submitter command authority** (audience ⊆ submitter, no destructive/widening side-effects — server+loop enforced, collapses confused-deputy). **Key-holder-only** placement (K1 operator→K3 controlled host; K4 hosted relaxed-E2EE reserved/ADR-gated). New risk fenced: free-form *research* commands → **SSRF-isolated outbound fetch in the loop** (not the device; ADR 0036 allowlist reuse). **Cost (spend-gated):** Sonnet default, batch-per-family-per-cadence, per-family daily cap, per-command research/token ceiling, image down-scale (~$2–6/mo/family dogfood; extends ADR 0025). Scopes: activate `intents:write` (member) / `intents:resolve` (loop). **EXPERIMENTAL/flagged**; client surface gated on the ADR 0008 `designs/two-way/Add-Context` mockup; promotion = separate operator decision. The largest-safety-surface, recurring-spend feature of W1–W5. Composes 0039/0016/0015/0017/0029/0030/0025/0036/0018. Design: `specs/two-way-engine-and-content-management-design.md` §4-W3. |
| 0035 | Block-Payload Schema Reconciliation — One Source of Truth | **Accepted** 2026-06-26 (operator "I accept 0035, 0033"); implementation (schema realign + regen + CLI/server block-payload validation) follows in its own PR. Was Proposed 2026-06-26 (agent-drafted from the authoring-doc audit behind #134 / `OQ-block-payload-schema`; **operator-gated** — edits the content schema). Hub **block** `payload` has two disagreeing definitions: the client render model (`Model.kt` — `docRef`/`domain`/`lat`/`lng`/`total`/`spent`) vs the generated schema (`Content.kt`←`content.schema.json` — `ref`/`source`/`kind`/`mapUrl`/richer `Item`). Server stores payload **JSONB verbatim** (passthrough) + CLI validates only block *type*, so the generated `BlockPayload` is **unused + drifted** — a structured block authored per the *schema* would silently not render. **Not yet hit** (live content is `body_md`-only → no migration). **Decision: Option A** — align `content.schema.json` to the client render model + regenerate `Content.kt` + add block-payload validation to CLI `validateHubTree` and the server (closing the "nothing guards it" root cause; mirrors the card parity lock #135). Rejected B (client→schema; churns working render code) + C (compat shim; entrenches ambiguity). Composes 0006/0022. Resolves `OQ-block-payload-schema`. |
| 0043 | Now Content Model — Derived + Authored Two-Lane Surfacing with an On-Device Priority Engine | **Accepted** 2026-06-30 (operator ratified in-session — proceed with Phase A; ADR 0008 hi-fi gate `designs/now-derived/` signed off same day). Was **Proposed** 2026-06-29 (agent-drafted from a 5-design-agent panel + requirements inventory; **operator-gated** — changes the Hub→Now boundary, softens the dumb-client posture, product-scope-shaped). **Splits "Now" into two lanes by the _provenance of its surfacing reason_** ("author the irreducible; derive the structural") and ranks both through **one on-device pure Priority & Ordering Engine** (the server never ranks Now). **Lane 1 — Derived:** client synthesizes ephemeral Now items on-device from already-synced hub content + block `triggers[]`/`places` against the live clock/location — no server persistence, no `/sync` rows, no write path, no `content.schema.json` entry (dumb-server invariant, ADR 0039 §7 tightens). **Lane 2 — Authored:** evolves `briefing_cards` (keeps the ADR 0022 typed renderers), gains an optional deep-link `target`, a `why`, a `trigger`, and a **bounded `importance`** weight (capped — no author-pinned spam; drops author-controlled ordinal). The engine = score (urgency/proximity/importance/reason-kind/anti-nag decay) → dedup/collapse by `subjectRef` → calm budget (soonest/nearest-N, now/soon/later bands, overflow never silently dropped) → deterministic stable order + hysteresis; uses **local-only, never-synced** surfacing state (last-shown/dismissed/quiet-hours — syncing it = a who-saw-what leak). Resolves `OQ-now-emission` (third answer: on-device derivation) + `OQ-notbefore-gating` (deriver gates `not_before`). **Phase A = foreground in-feed only** (no background location/notifications/new permission — that's Phase B). Composes 0006/0014/0015/0020/0022/0030/0038/0039/0040/`scope-and-access-model.md` §7. Design: `specs/now-content-model-design.md`. |
| 0044 | Phase B — Background-Location & Local-Notification Surfacing Posture | **Accepted** 2026-06-30 (operator "Accept ADR 0044 as written" → Gate B closed) — **BOTH GATES CLOSED → BUILT + SHIPPED to `main` (PR #260)**: Gate A design sign-off CLEARED 2026-06-30 (v2 `designs/triggers/` mockups + INB-13 §6b honesty rework, false-coords claim removed; INB-13 closed) + re-approved as-shipped 2026-07-01. Local-only Android Phase B: headless background pass, `AndroidLocalNotifier`, `GeofencingClient` + `AlarmManager` scheduling, `NotifConfig` quiet-hours + daily-cap (device-local, never-synced, default-OFF), permission ladder, offline/geo-on states. **Open (not blocking):** iOS parity (unbuilt, needs Xcode); **activity** trigger stays deferred (schema slot; operator 2026-07-01); public-ship Play/App-Store background-location declaration + disclosure review (pre-public). Was **Proposed** 2026-06-30 (agent-drafted at the ADR 0043 Phase-B build gate; **operator-gated — HARD GUARDRAIL #3/#4**: new "Always" background-location permission + customer-disclosure review). **Extends ADR 0043 Phase B + concretizes ADR 0014 §4/§5/§6** (the "later milestone" the trigger ADR reserved). Phase B turns the *already-built* shared engine (ADR 0043 §2b — notifications = top-K of the same `rank()`) into background notifications when the app is closed, entirely on-device: **background geofence** nearest-N (iOS ~20-region cap, ADR 0028), matched on-device with **live position never leaving the device** (ADR 0014); **LOCAL notifications** (Android `NotificationManager` / iOS `UNUserNotificationCenter` — **no FCM/APNs, no server push, no server change**; dumb-server invariant ADR 0039 §7 intact). Adds the two Phase-A-deferred `RankConfig` knobs — **quiet-hours** + **daily notification cap** — as **device-local, never-synced** config (ADR 0024; `rank()` stays pure, config injected). "Always" = opt-in / reversible / never up-front (ADR 0014 §4); permission state never syncs; notification-tap deep-links via Phase-A `CardAction.OpenHub`. **No vendor, no recurring cost** (local notifs + OS geofencing are free). Composes 0043/0014/0028/0024/0039/0015/0017. Design brief: `designs/DESIGN-BRIEF-triggers.md §6b`. |
| 0036 | External-Image-URL Privacy Posture — HTTPS Host-Allowlist + Hardened Parser (Phase 1) | **Accepted** 2026-06-26 (operator-directed "Accept — Wikimedia-only start"; customer-data-handling class, guardrail #3/#4) — author-supplied image fields (`Hub.media`/`BriefingCard.media`/block `thumbnailUrl`/`avatarUrl`) validated by one **shared hardened rule** server+client+CLI: **https-only**, reject userinfo `@`, punycode `toASCII`-normalize host, port empty/443, **exact-host allowlist** (not suffix), reject SVG, length-cap. **Phase-1 allowlist = exactly `upload.wikimedia.org`** (Wikimedia Commons); additions one exact host at a time, operator-approved, no wildcards. Accepts an **explicitly-temporary** client→third-party IP/usage leak, eliminated by **Phase 2** self-host+CDN (deferred). Skill reads the bundled allowlist+curated-icon constants and **surfaces its chosen image for operator confirm before push** (trademark/wrong-entity guard). Cleared the enrichment image-render BUILD gate (with ADR 0008 hi-fi signoff). Composes 0004/0006/0022/0029/0030. Spec: `specs/hub-card-visual-enrichment-design.md`. |
| 0046 | Client-Derived Timeline Fallback — a Second On-Device Projection over Hub Content | **Accepted** 2026-07-01 (operator "Accept + build"; **both gates closed** — Gate A `designs/derived-timeline/` mock + Gate B ADR ratified; 5 decisions resolved: all-four sources, render-only, "From this hub's dates", family→device tz, "label" source depth + most-specific-date-wins). Was **Proposed** 2026-07-01 (agent-drafted at the ADR 0045 Phase-2 gate the parent reserved; **operator-gated** — softens the dumb-client posture + re-touches the Hub→Now boundary, ADR-0043/0045-class). Would add a pure `deriveTimeline(hub, clock, tz)` in `commonMain` (mirrors `deriveNow`) that, **only when a hub has no authored `Hub.timeline`**, projects the hub's already-synced **dated blocks** (checklist `due`, `milestone` `date`, location pickups, hub `countdown_to`/`start_at`/`end_at`) into synthetic stops, rendered through the **same** ADR 0045 `TimelinePresenter`/card/detail (zero new render code; **authored always wins**). No schema/server/write-path change (pure client, like `deriveNow`) — but it is the **second on-device projection over hub content** (the crux: further softens the dumb-client posture, ADR 0015/0043 — operator's call). Five open decisions for the operator: (1) source-block set; (2) **Now-feeding** — proposed **render-only** like Phase 1 (no `deriveNow`/notification, avoids double-surfacing); (3) provenance flips to honest **"Built from this hub"** (vs authored "Added to this hub" — ADR 0014/0015 guardrail); (4) tz = family→device fallback (no author `tz`); (5) accept the second projection. Requires a hi-fi mock of the derived/empty states (ADR 0008 Gate A) before build. Composes 0045/0043/0006/0035/0014/0015/0044/0030/0022. ADR: `adr/0046-derived-timeline-fallback.md`. |
| 0045 | Hub Timeline — Authored Hub Property with On-Device Presentation | **Accepted** 2026-06-30 (operator "0045 accepted"; Gate B closed; **Gate A ADR-0008 mock sign-off CLEARED** same day — revised mock pushed to claude.ai/design + operator-signed-off → cleared for build). Was **Proposed** 2026-06-30 (agent-drafted from the imported `designs/hub-timeline/` mock + a 6-agent review; **operator-gated** — new authored content representation over hub content + a new on-device presentation projection; ADR-0035/0006-class). Renders a hub timeline (two scales: day rail + hub roadmap) from a new **authored, content-blind `Hub.timeline` property** (NOT a block — one-per-hub free via the hub PK; renders as a hoisted dossier card; old clients ignore an unknown hub property cleanly). Seam = *author the irreducible, derive the structural*: the **author** (Claude skill/CLI/API) writes the **stops**; the **client** computes status/NOW/scale/grouping/windowing/collapse as a pure injected-clock+tz function (`TimelinePresenter`, commonMain, mirrors `deriveNow`); **server stores + structurally validates only** (dumb-server invariant intact). **Source model C, A-first** — authored now; client-side **derived fallback deferred to Phase 2** (its own ADR — the ADR-0043-class second projection). **Phase 1 = render-only, Now-invisible, zero notifications**; provenance copy reflects **authored** origin (no "derived on-device" claim until Phase 2 — honesty guardrail). **Author-stamped IANA `tz`** (client falls back family→device) protects the multi-member wedge. Typed attachments reuse `cardActionUri` (tel/geo/https) + a typed in-app `open`→`CardAction.OpenHub` (no `df://`); stop times reuse RFC-3339. Timeline = projection not worklist (stops not member-checkable; card supports "Hide for me"). **Platforms Android/iOS/Desktop** (Web out — no wasmJs target). **Two gates open:** Gate A = revised `designs/hub-timeline/` ADR-0008 sign-off (honest copy, one-card IA, state mocks); Gate B = ratify this ADR. Fixes the mock's mis-cites (write model 0038/0039 + author posture 0030, not 0008) + "never a notification" (ADR 0044). Composes 0043/0035/0006/0030/0038/0039/0014/0015/0017/0044/0004/0022/0008. Spec: `specs/hub-timeline-design.md`. |
| 0047 | Client `:ui` Module Extraction — Incremental Split of the Monolithic `:client` | **Accepted** 2026-07-02 (operator accepted as written — "yes"; Phase 2 cleared). Was **Proposed** 2026-07-02 (agent-drafted from a measurement pass + a two-round expert panel — design 4-agent + plan 3-agent; **operator-gated** — module/platform architecture + maintenance-burden, ADR-class). Operator directed the work, chose incremental scope, and at the Phase-1 gate directed "proceed with the split." Extract a Compose **`:ui`** KMP module (~40 Compose/coil files + theme + fonts + 3 UI expect/actual seams + iOS framework entry) from the monolithic `:client`; `:ui` `api`-depends `:client`; engines/data/store/SQLDelight stay — **no dependency inversion**; `:client` ends Compose-free + emits no framework. **Driver (measured):** every incremental edit recompiles **~all 115 files** via the **KT-62686** IC safety fallback (~5s/edit); cheap Gradle levers don't move it (build-cache regressed → reverted; config-cache ~0.15s). Split = smaller compile unit + **structural KT-62686 mitigation** (Compose-free `:client` expected to escape the fallback → sub-second logic-edit loop) + **test isolation** + `:client` sheds Compose + seeds `:model`/`:data` + web target. **Costs:** full/CI build ~+5–15% (2 compile tasks + double config + extra klib link — *number deferred, needs the `google-services.json` secret*); two KMP modules; `internal`→`public` promotions (`selectScale`/`cardToNowItem`); zero core encapsulation from apps until the deferred `:model`/`:data` slice. Rejected: full 3-way split (dependency inversion), `:client`→`:core` rename, direct KT-62686 workaround. Composes **TASK-KMP**; relates **CL-SNAP** (#277 — snapshot tests → `:ui`). ADR: `adr/0047-client-ui-module-extraction.md`; design `specs/client-modularize-ui-extraction-design.md`; plan `docs/superpowers/plans/2026-07-02-client-modularize.md`; evidence `specs/client-modularize-measurements.md`. |
| 0048 | Invite Deep-Links — Android App Links now, iOS Universal Links deferred | **Accepted** 2026-07-07 (operator-directed in-session — "review the ADR gated decisions… lets unblock this"; chose *no Apple account yet* + *ADR + build Android now*). **Supersedes the invite portion of ADR 0011 §1's App-Links deferral** (that was prototype-scoped; App Links already SHIPPED for the S6-D device-grant flow — this extends the accepted, live pattern to invites). The minted invite URL `…/invite/{token}` currently **404s** (no route/handler); paste-into-Join is the only working redeem path. Decision: **Android invite deep-links in scope NOW** — add `GET /invite/:token` browser landing (mirrors `GET /device`) + `/invite` to the AASA paths; add `path="/invite"` to the verified `autoVerify` intent-filter (same host/cert, **assetlinks.json unchanged** — `handle_all_urls`); `MainActivity.handleDeepLink` → new `AuthEngine.openInviteLink()` (stash-and-consume-after-sign-in mirror of `openDeviceLink`, reuses `redeemInvite`/`JoinInviteScreen`). **No new capability/spend/domain.** **iOS Universal Links DEFERRED** until an Apple Developer account (Team ID / `APPLE_APP_ID`, ~$99/yr, guardrail #6 — shared with Apple Sign-In ADR 0023) is provisioned; then set the env, activate the AASA `/invite` path, add the iOS entitlement + handler. Domain stays `family-ai-dashboard.vercel.app` (branded consumer domain = later A6 re-point, not a blocker). Security unchanged — deep-link only transports the token into the same authed `POST /invites:redeem` (uniform-404, per-account lockout, owner-approved-pending). Composes 0011/0023 + spec `05-invite.md` §41–45. ADR: `adr/0048-invite-deeplink-android-first.md`. |
| 0049 | Content-Authored Geofences — proximity posture for authored triggers | **Accepted** 2026-07-08 (operator ratified in-session — chose **Option A**). Resolves the sub-question ADR 0044 §2 / ADR 0014 §4 left open when issue #299 consumes an authored `BriefingCard`/hub-block `geo` trigger: may *content-authored* (Claude/CLI-pushed) regions arm **background** geofences? **Option A: no.** Background proximity stays **user-curated** — an authored trigger reaches the background only via `place_ref` to a place the family already saved (which is already geofenced via `activePlaces()`); a coord-only authored geo trigger gets **foreground** on-device surfacing (while-using permission, no OS geofence) but arms no background geofence. So every OS background geofence remains a user-curated, user-visible region → **ADR 0044's disclosure posture does not move**, no per-family cap / disclosure-copy / data-safety update needed. The **time** path (`when.at` + `alert_offset` → banding + local exact-alarm notify) is adopted unconditionally (no location, no permission). On-device matching / no location egress (ADR 0014) invariant. **Option B** (content-authored coord background geofences, gated on a per-family cap + honesty affordance + disclosure review) recorded but **not** adopted. No vendor/spend. Concretizes 0044/0014. Issue #299; design `docs/superpowers/specs/2026-07-08-authored-card-triggers-design.md`. ADR: `adr/0049-content-authored-geofences-posture.md`. |
| 0050 | Card↔Detail Container Transform Uses Plain `AnimatedContent`, Not `SeekableTransitionState` | **Accepted** 2026-07-08 (operator-directed in-session — chose the **commit-animated** fix). The card→detail "fold" morph (ADR 0022 D3) and the hub card→timeline morph (ADR 0045 §13b) rendered as a **flat crossfade / flash**, not a container transform. Root cause: #237 (`18d0988`) rebuilt the transition on a `SeekableTransitionState` for predictive-back finger-scrubbing, and **driving a shared-element `AnimatedContent` from a `SeekableTransitionState` silently drops the `sharedBounds` morph** — the detail snaps to full-screen bounds; only a fade plays. Confirmed **identical on androidx.compose.animation 1.11.2 / 1.11.3 / 1.12.0-alpha03** → a Compose-API limitation, not a version bug (keeping both the morph *and* the finger-scrub is not achievable today). **Decision:** drive both transforms with a plain state-driven `AnimatedContent(targetState=…)` (restores the morph); **predictive back is commit-animated** — OS window peek during the drag, reverse morph on release, no 1:1 finger-scrub (that scrub never morphed anyway). Reduced motion → `dur=0` snap. `PredictiveBackMotion.kt` helpers retained for a future live-drag (option B, deferred). Refines 0022/0045 (upholds both morphs), composes 0047. No vendor/spend. Verified on-device (open + reverse morph) + `:ui:desktopTest` green. ADR: `adr/0050-container-transform-plain-animatedcontent-not-seekable.md`. |
| 0051 | Navigation Transition System — Taxonomy, Tokens, Central Route-Motion Host | **Accepted** 2026-07-08 (operator-directed in-session — chose **expressive/spatial** motion + **full system + persistent shell** in brainstorming). Audit found only **2 of ~20 nav edges animated**; the rest (Feed↔Hubs tab switch, Feed→Account, Account→settings pushes, hub list→detail, device-link wizard, every back) were instant cuts, with no shared vocabulary/tokens and nothing making *future* routes animate by default. **Decision:** a motion **taxonomy** (Tab=shared-axis-X, Push/Pop=shared-axis-Z, Modal=slide-up, Wizard=X, Gate=fade-through; container transforms = the **Hero** tier) driven by three isolated units — `RouteMotion.kt` (pure `routeSpec` **exhaustive `when` over `Route`** + unit-tested `navAnimFor` resolver; tier-gated modal rule), `NavMotion.kt` (duration/easing tokens + the one `NavAnim.toContentTransform` builder; hero durations tokenized to 460/400/0), and **`AppNavHost`** (one `AnimatedContent` wraps the router; Feed+Hubs share a `"tabs"` key; exiting screen uses the frozen `route`). A **persistent `TabShell`** (Column, not nested Scaffold) keeps the bottom bar put during the shared-axis-X tab slide and route-scoped `barVisible` hides it for full-screen details (preserving ADR 0050's morph). **Standing rule:** every `Route` MUST have a `routeSpec` entry — new nav picks a `NavKind`, it does not hand-roll a transition (a missing entry won't compile). Refines 0022 (D3)/0045 (§13b)/0050 (Hero tier), composes 0047. No nav library, no vendor/spend. Verified on-device (tab slide + persistent bar + account modal) + `:ui:desktopTest` green (479 tests, incl. resolver unit tests). Design: `docs/superpowers/specs/2026-07-08-navigation-transition-system-design.md`. ADR: `adr/0051-navigation-transition-system.md`. |
| 0052 | DB-First Cold-Start Route Gate — Cache Memberships So the Splash Doesn't Wait on the Network | **Proposed** 2026-07-09 (agent-drafted from a cold-start investigation; operator-gated, not built). Content is offline-first + correct (ADR 0020: DB is source of truth, cached cards render on frame 1, zero network), **but the top-level splash route gate is network-gated**: `Route.Loading` clears only on `MembershipsLoaded`, dispatched **only after a successful network `whoami`** (`AuthEngine.kt:352-355`). Root cause — `state.families` lives only in memory, sourced only from `whoami`; **never persisted locally** (no `membership` table in `Content.sq`; `TokenStore` persists only the `Session`). So cold start (process reclaimed) blocks first paint of already-cached content for a `whoami` round-trip, and offline it falls to `Route.AuthError` withholding a good cached view. **Decision:** persist last-known memberships in a SQLDelight **local-only `membership` table** (reuses `DriverFactory`+migrations, zero new per-platform code); on cold start with token **+** cached memberships, `AuthEngine.restore()` reads them via a **synchronous one-shot query** (a seam lambda, no `ContentStore` dep) and dispatches **one atomic `MembershipsRestored(session, families)`** → route **straight to `Route.Feed`** off a local read (splash still frame-1 but flips with no network round-trip). `whoami` reconciles in the **background** (`AuthEngine` gains a scope; mutex taken inside the launched job): success → overwrite+re-`routeFor`; 401 → clear token+cache→`SignIn`; **network/5xx → stay on `Feed`** with cached view, not `AuthError`; `AuthError` reserved for token-but-empty-cache. **Single-writer preserved** — memberships are NOT flow-bridged through `SyncEngine` (would race `SessionRestored` + double-write `families`). Accepts a bounded **optimistic-render window** (one round-trip of already-on-device own-family data before session re-confirmation — same boundary the content cache already accepts, §4). Pure client; content-blind server untouched (no schema/route/write-path change). Composes 0011/0021/0023 (auth arch — caches its output), 0020 (extends DB-source-of-truth to the route gate), 0015 (content-blind, untouched), 0033 (migration), 0040 (cache-clear boundary). Membership cache wiped on **both** revocation detectors (auth-401 **and** sync-403/404, ADR 0030 P0-2 — which race but must converge on `SignIn`+wiped) but **survives** the `reconcileSchemaVersion` content-heal (two distinct wipe scopes). **Gate A** (ADR 0008): no new screen; one small new affordance = quiet offline/"last updated" indicator on the network-fail path — wants a design nod. **Gate B** = operator ratifies the optimistic-render posture (§4) + membership-cache home (DB table recommended vs. `TokenStore` rejected-alt). ADR: `adr/0052-cold-start-db-first-route-gate.md`. |
| 0053 | Per-Hub Participation Roles (Viewer / Contributor / Co-owner) & Delegated Hub Management | **Accepted** 2026-07-10 (operator-directed in-session — "ADR 0053 is accepted"; agent-drafted, operator-ratified). Implementation tracked as Delta C (P2/P3). Companion to the ADR-0008-signed-off design `designs/Account-ACL.dc.html`. **Extends — does not supersede — ADR 0030**, which shipped a *read-only* hub allow-list (`resource_visibility`), author-always-permitted, **family-owner-NOT-auto-permitted**, and explicitly *no new role* (item 7). The operator asked that hub owners let others **participate** (contribute content + delegate who's in a hub), with a **simplified** admin (no permission matrix). **Decision:** add a **per-resource** role — a fourth column `resource_visibility(family_id, hub_id, user_id, role)` with `role ∈ {viewer, contributor, co_owner}` — **not** a new family role (the `memberships.role` owner|adult enum stays untouched, so ADR 0030 item 7 holds). **Viewer** = today's read grant (migration default → zero behavior change); **Contributor** = read + write via the two-way engine (ADR 0038/0039), where write still resolves through **ADR 0029 `hub:<id>:write` scope** (role is the *source*, `requireScope` the *gate* — the three-axis read/write separation is preserved) and a Viewer never gets write; **Co-owner** = contribute + **manage people** (add/remove, set role, flip `family`↔`restricted`). The **hub author is a permanent, non-removable implicit Co-owner** (resolved from `hubs.created_by`, ADR 0030 2a; NULL-author M0 hubs get no implicit owner) → **no last-manager orphan state**. **Family-owner-not-auto-permitted preserved** (the co-parent/eldercare privacy call) — the designed **"no access"** state covers the family owner. **ADR 0030 revocation/tombstone mechanic preserved and extended to `role` UPDATEs** (mutation touches `hubs.updated_at` → keyset re-surfaces tombstone; a **contributor→viewer downgrade = `hub:<id>:write` scope revocation**). Roles are **hubs-only**; cards keep their flat author-stamped `audience` (ADR 0030 item 3, untouched). **Viewer is MVP; Contributor/Co-owner + in-app People management = the post-MVP in-app-authoring slice ADR 0030 deferred, designed now (ADR 0008), built with 0038/0039.** Rejected: read-only-only (fails the ask); a new *family* role (violates 0030 item 7); a capability matrix (the "overwhelming" option); granting write via the visibility filter (collapses the axis separation). Composes 0011/0029/0030/0038/0039. Spec to update on acceptance: `specs/domain-model/scope-and-access-model.md`. ADR: `adr/0053-per-hub-participation-roles-and-delegated-management.md`. |
| 0054 | SWIP Bug Reporter in Debug Builds — Shake → Capture → Annotate → Review, Zero Release Footprint | **Proposed** 2026-07-10 (agent-drafted with the `swip-bugreport-wiring` PR; accept on merge). Wires the published SWIP bug reporter (`works.sloop.swip:*:0.1.0`, GitHub Packages) into `:androidApp` **debug builds only**, mirroring the proven `debugDrawerPlugins()` per-variant idiom: `src/main` calls `bugReporterEnhancer`/`bugReporterInstall`/`BugReporterWrapped`; `src/debug` wires the real SDK (`debugImplementation`); `src/release` is an inert same-signature mirror → release APK carries **zero swip bytes** (verified by an `assembleRelease` + classpath gate). `:client` stays swip-free — `createAppStore` gains an optional `extraEnhancer` composed rightmost/innermost (the recorder's required slot). New `:swip-wiring` KMP module owns the privacy **allowlist slice registry** (`route`, `syncing`, `detailStack` ids, derived `cardsCount`, `hubFilter` chip — NEVER `session`/`members`/`myDisplayName`/card content/error strings), the `dayfoldSanitizer` (drops `eyJ`/`@`-bearing values; truncates `hubFilter` to 32), and the **mandatory salted leak test** — a CI gate (`:swip-wiring:desktopTest`). Anonymous identity, no upload (SWIP gateway = its Phase 1); reports land in the on-device lane (`noBackupFilesDir/swip-reports`, 3/15 MB/7 d budget). Requires the `SLOOPWORKS_PACKAGES_TOKEN` CI secret + local `gpr.*` PAT. SWIP follow-up filed: okio should be `api` in swip-bugreport (0.1.0 leaks okio types through `ReportLane`'s ctor). Rejected: vendor SDKs (lock-in, network SDKs in a family-data app); release-flag wiring (violates zero-footprint for no benefit pre-gateway). ADR: `adr/0054-swip-bugreport-debug-build-wiring.md`. |
| 0055 | SWIP Product Analytics in Debug Builds — Live PostHog EU, Count-Only, Zero Release Footprint | **Proposed** 2026-07-11 (agent-drafted; accept on merge). Wires live product analytics via the published SWIP KMP SDK (`works.sloop.swip:{swip-core,schema-dayfold}:0.1.2`, swip-rk redux mapper-table pattern), **composing with ADR 0054** — analytics shares `:swip-wiring` and the `createAppStore(extraEnhancer)` slot (composed with the bug recorder in the debug glue, not replacing it) — and **realizing ADR 0015**'s vendor-account gate: live **PostHog EU** (`https://eu.i.posthog.com`), analytics-only (no Sentry keys → errors/config/telemetry facades stay NoOp; `swipTimingEnhancer` omitted). **Variant scope: DEBUG/internal-only** — `phc_` project key is a debug-only BuildConfig field from Infisical `/dayfold`; the release APK carries **zero swip-analytics bytes** (inert `src/release` glue, `javap`-verified). **Count-only 8-event slice 1**: `account_signed_in`, `signed_out`, `family_created`, `invite_redeemed`, `invite_rejected` (closed-enum `reason`), `hub_opened`, `card_opened`, `sync_failed`, plus the free swip-lifecycle trio (`app_foregrounded`/`app_backgrounded`/`screen_view`) — **no identifiers, no free-text, no PII** (session/family-name/error-message/hub-card-ids all dropped at the mapper); `checklist_item_toggled` deferred (toggle is an outbox effect, not a redux action — needs a new thin action). **Privacy hardening:** PostHog server-side **geoip disabled at the transport** (`$geoip_disable:true`+`$ip:"0.0.0.0"`, a SWIP-core 0.1.2 change) and Dayfold **never calls `identify()`** with PII — `distinct_id` stays the opaque SWIP install id. **`:client` stays SWIP-free** (mapper table + NoOp facades live in `:swip-wiring`; `Swip.init`+transport+lifecycle live in the androidApp debug glue). **Scope: ratified only for the operator's dogfooded household** — widening to release/real users is a **future ADR** gated on a privacy-policy disclosure + consent surface wired to `CollectionMode` (guardrail #3/#4). Rejected: shipping in release now; id-bearing events at slice 1; a hand-written event type (SWIP codegen is the source of truth); leaving geoip on. Design: `docs/superpowers/specs/2026-07-11-swip-analytics-integration-design.md`. ADR: `adr/0055-swip-analytics-integration.md`. |
| 0056 | SloopLogging Integration — Leveled, Scrubbed, On-Device Logging | **Proposed** 2026-07-12 (agent-drafted with the `sloop-logging` integration work; accept on merge). A leveled `Log` front-door lives in `:client` (SWIP-free by design, 4 levels — debug/info/warn/error, parity with `@sloopworks/swip-logging`), lazy inline messages, `println` fallback when unbound. SWIP-owned `SloopLogging` (`swip-logging:0.1.1` / `swip-core:0.1.3`, ADR-0025 in SWIP) is bound behind `Log.sink` in the `:androidApp` **debug** glue only (console + devtools-drawer writers), mirroring ADR 0054's per-variant idiom — release keeps the `println` fallback, floored at WARN+, zero swip bytes. The **PII scrubber runs in SloopLogging ahead of every writer**; the ADR closes a leak where the ADR-0054 bug-reporter breadcrumb ring captured pre-scrub raw text — it now runs `scrubString` too, so the ring and the writers are provably consistent. On-device-only egress (no upload path); level control is local-first (`Log.minLevel`) with a future `SloopConfig` remote seam reserved but not built. Seeded modest engine logging (auth/sync/hub/now milestones) at B5. **Deferred:** the analytics-mirror hook (needs the analytics wiring on `main`), a per-tag drawer level toggle, wiring `apps/api` to the TS `@sloopworks/swip-logging`. Composes 0047 (:client core), 0054 (shares the debug glue + breadcrumb ring), 0055 (analytics). ADR: `adr/0056-sloop-logging-integration.md`. |
| 0057 | SWIP Debug Inspector Plugin — Live Analytics Timeline in the Debug Drawer, Mask-by-Default | **Proposed** 2026-07-12 (agent-drafted with the `swip-inspector-plugin` PR; accept on merge). New KMP module `apps/debugdrawer-swip` — a Compose `DebugPlugin` (`id="swip"`) rendering the Phase-1 capture engine's `works.sloop.swip:swip-debug:0.1.0` `RingDebugSink.entries` stream as a flat, live, newest-first timeline in the existing debug drawer (ADR 0019); swip-core bumped 0.1.2→0.1.3 for the `debugSink` install seam (`SwipPlatformDeps.debugSink`, injected via `.copy()` on the codegen'd `platformDeps`). Wired `debugImplementation` from `:androidApp` **only** → **zero swip-debug bytes in release** (verified by the release compile). **Install-gate: an allowlist on `BuildConfig.DEBUG`** (TODO to gate on channel ∈ {dev,ci} later — never a `!=` prod blocklist, since beta ships to real users); the `RingDebugSink` is created once in the debug glue holder and shared between plugin registration and `Swip.init(debugSink=…)`. **Privacy: mask-all-by-default** — prop values + ids (`distinctId`/`sessionId`/`eventId`/`eventIds`) render as `••••` with reveal-on-tap; copy honors the mask state; no per-field `privacy_class` in v1 (deferred — needs a field→class codegen map SWIP doesn't emit yet). **Capture isolation**: while a value is revealed, the host sets the Activity window `FLAG_SECURE`; the ADR-0054 bug reporter's `PixelCopy` capture **honors `FLAG_SECURE`**, so an open inspector's raw PII can't land in a screenshot or dogfood bug bundle — no bug-reporter change needed. **Scope: flat timeline v1, Android debug only** — deferred: per-event journey folding, per-`privacy_class` chips, free-text search, iOS/desktop parity. Memory-only, never transmits (inherited from the Phase-1 engine). Mandatory on-device smoke test (chrome insets, `FLAG_SECURE` screenshot blanking) deferred to the operator. Composes 0054 (bug reporter, shares the capture path), 0055 (analytics, shares `:swip-wiring`/debug-glue seam). ADR: `adr/0057-swip-debug-inspector-plugin.md`. |
