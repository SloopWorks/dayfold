# Roadmap

Execution planning. Pre-G3 this directory is mostly empty by design.

After Gate G3/G4 it gains: milestone map + dependency graph, `milestones/`
files with acceptance criteria, an unknowns register, and per-milestone
briefings — run via the 8-phase build workflow (ADR 0002).
